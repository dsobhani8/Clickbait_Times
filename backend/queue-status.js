console.log("[analytics-queue:status] Client analytics queue is stored on-device (AsyncStorage).");
console.log(
  "[analytics-queue:status] Read current queue size from Expo logs: look for lines like `[analytics-queue] queued=...`."
);
console.log(
  "[analytics-queue:status] Server-side status is best checked via `/dashboard/ops`, `/health`, or direct SQL against Supabase Postgres."
);
